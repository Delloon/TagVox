# TagVox

![License: AGPL v3](https://img.shields.io/badge/license-AGPL--3.0-blue.svg)
![Rust](https://img.shields.io/badge/rust-1.83%2B-orange.svg)

Discord-бот на Rust (`serenity` + `songbird`). Реагирует на упоминание
набором настраиваемых фраз, по голосовым командам подключается к
голосовому каналу и воспроизводит локальные аудиофайлы с поддержкой
паузы/возобновления и кнопок управления в чате.

## Содержание

- [Возможности](#возможности)
- [Требования](#требования)
- [Установка зависимостей сборки](#установка-зависимостей-сборки)
- [Сборка](#сборка)
- [Настройка Discord-приложения](#настройка-discord-приложения)
- [Файлы настроек](#файлы-настроек)
- [Запуск](#запуск)
- [Команды в Discord](#команды-в-discord)
- [Кнопки управления](#кнопки-управления)
- [Дебаг и диагностика](#дебаг-и-диагностика)
- [Протокол DAVE](#протокол-dave-обязательное-сквозное-шифрование-голоса)
- [Структура проекта](#структура-проекта)
- [Лицензия](#лицензия)

## Возможности

- **Реакции на упоминание.** При упоминании бота сообщение сравнивается с
  триггерами из `phrases.json` нечётким сравнением (учитываются похожие
  формулировки, а не только точные совпадения), и в ответ отправляется одна
  из заданных фраз.
- **Голосовые команды.** Если фраза похожа на одну из `voice_commands`, бот
  подключается к голосовому каналу, где находится автор сообщения, и
  воспроизводит привязанный к этой фразе локальный аудиофайл.
- **Пауза и возобновление.** Позиция трека сохраняется автоматически (через
  `songbird`), пока трек не остановлен явно.
- **Кнопки управления.** Под сообщениями о воспроизведении появляются
  интерактивные кнопки «Пауза» / «Продолжить» / «Стоп» — они работают в том
  же канале, где была вызвана команда.
- **Неограниченное количество реакций и команд** — всё задаётся в JSON.
- **Автоматическая настройка при первом запуске.** Токен запрашивается один
  раз через консоль и сохраняется локально; настройки бота создаются
  автоматически со значениями по умолчанию.
- **Автокопирование ресурсов при сборке.** `phrases.json` и папка `sounds/`
  копируются в папку с исполняемым файлом автоматически при сборке.

## Требования

- Rust (стабильная версия, 1.83 или новее) — https://rustup.rs
- CMake — нужен для сборки библиотеки Opus (аудиокодек, который использует
  Discord)
- На Windows — компилятор C/C++ (Visual Studio Build Tools с компонентом
  «Desktop development with C++», либо полная Visual Studio)
- `ffmpeg` — используется `songbird` для декодирования аудио
- Discord-приложение с включённым **Message Content Intent**

## Установка зависимостей сборки

### CMake

CMake необходим, так как крейт, отвечающий за поддержку кодека Opus,
собирает библиотеку Opus из C-исходников.

1. Скачать установщик с https://cmake.org/download/ и установить.
2. При установке отметить опцию добавления CMake в PATH (для всех
   пользователей или для текущего пользователя).
3. Открыть новую сессию терминала (важно — старая сессия не увидит
   обновлённый PATH) и проверить: `cmake --version`.

Если сборка завершается ошибкой вида
`Compatibility with CMake < 3.5 has been removed from CMake` — значит,
установлена версия CMake 4.x, а исходники Opus указывают устаревшую
минимальную версию политики. Обход:

```powershell
# PowerShell (действует только в текущей сессии терминала)
$env:CMAKE_POLICY_VERSION_MINIMUM="3.5"
```

```bat
:: CMD
set CMAKE_POLICY_VERSION_MINIMUM=3.5
```

Чтобы не задавать переменную каждый раз, её можно добавить в системные
переменные окружения постоянно (Панель управления → Система → Дополнительные
параметры системы → Переменные среды).

### ffmpeg

- Ubuntu/Debian: `sudo apt install ffmpeg`
- macOS: `brew install ffmpeg`
- Windows: скачать сборку с https://ffmpeg.org и добавить путь к `ffmpeg.exe`
  в PATH

### C++ Build Tools (только Windows)

Если сборка завершается ошибкой об отсутствующем компиляторе, необходимо
установить компонент «Desktop development with C++» через Visual Studio
Installer (Visual Studio Build Tools или полная Visual Studio подходят
одинаково).

## Сборка

```bash
git clone https://github.com/<владелец>/tagvox.git
cd tagvox
cargo build --release
```

При сборке (см. `build.rs`) `phrases.json` и папка `sounds/` автоматически
копируются в `target/release/` (или `target/debug/`), если их там ещё нет.
Уже существующие в целевой папке файлы не перезаписываются, поэтому
локальные правки, внесённые прямо в `target/release/`, сохраняются между
пересборками.

## Настройка Discord-приложения

1. Создать приложение на https://discord.com/developers/applications.
2. В разделе Bot → Privileged Gateway Intents включить **Message Content
   Intent**.
3. Через OAuth2 → URL Generator сформировать ссылку приглашения со scope
   `bot` **и `applications.commands`** (второй scope обязателен — без него
   слэш-команда `/anon` не появится в Discord) и правами: View Channels,
   Send Messages, Read Message History, Connect, Speak.
4. Перейти по ссылке и выбрать сервер для добавления бота.

Если бот уже был приглашён раньше без scope `applications.commands` —
нужно повторно пройти по новой ссылке приглашения (с этим scope) для того
же сервера; повторное приглашение не создаёт дублей, только добавляет
недостающее разрешение.

## Анонимные сообщения (/anon)

Слэш-команда `/anon <текст>` публикует сообщение в настроенном канале от
имени постоянного псевдонима («🕵️ Аноним #42» — один и тот же номер у
одного и того же пользователя на этом сервере при каждом сообщении), не
раскрывая обычным участникам Discord-имя или ID автора. Внутри бот всё
равно знает, кто написал — это нужно для модерации и защиты от
злоупотреблений; анонимность действует только относительно участников
сервера.

Настраивается per-guild (канал физически привязан к конкретному серверу) в
веб-панели → «Анонимные сообщения»: выбор канала, кулдаун между сообщениями
одного пользователя, список ролей, которым разрешено пользоваться командой
(по умолчанию — никому, пока админ явно не выберет роли). Глобально
включить/выключить модуль сразу на всех серверах можно в веб-панели →
«Модули».

Требует scope `applications.commands` при приглашении бота (см. выше). БД
(`panel.db`) инициализируется автоматически при каждом запуске независимо
от того, включена ли сама веб-панель (`panel.toml` → `enabled`) — она нужна
не только панели, но и `/anon` (роли, кулдаун, псевдонимы), поэтому
работает даже если панель выключена.

## Файлы настроек

### `config.json` (создаётся автоматически при первом запуске)

```json
{
  "command_prefix": "!",
  "similarity_threshold": 0.78,
  "require_mention_for_reactions": true,
  "sounds_dir": "sounds"
}
```

- `command_prefix` — префикс управляющих команд (`!pause`, `!resume` и т.д.).
- `similarity_threshold` — порог похожести фразы на триггер (0.0–1.0). Для
  распознавания более разнообразных формулировок значение снижается; для
  более строгого совпадения — повышается.
- `require_mention_for_reactions` — если `false`, реакции и голосовые
  команды срабатывают без упоминания бота, на любое сообщение.
- `sounds_dir` — папка по умолчанию для звуковых файлов, если в
  `phrases.json` в поле `file` указано только имя файла без пути.

### `token.json` (создаётся автоматически при первом запуске)

При первом запуске исполняемого файла в консоли запрашивается токен бота
(ввод скрыт). После ввода токен сохраняется в `token.json` рядом с
исполняемым файлом; при последующих запусках запрос не повторяется.

```json
{ "token": "токен_бота" }
```

Этот файл содержит секретные данные и не должен публиковаться или
попадать в систему контроля версий (исключён в `.gitignore`).

### `phrases.json` (редактируется вручную, пример входит в репозиторий)

```json
{
  "text_reactions": [
    {
      "id": "greeting",
      "triggers": ["привет", "хай"],
      "responses": ["Привет!", "Здорово!"]
    }
  ],
  "voice_commands": [
    {
      "id": "sad_song",
      "triggers": ["включи грустную песню"],
      "file": "sad_song.mp3"
    }
  ],
  "unknown_response": ["Не понял, что имеется в виду."]
}
```

Поле `file` может содержать либо имя файла (тогда путь берётся из
`sounds_dir`), либо полный/относительный путь со слэшами. Количество
записей в `text_reactions` и `voice_commands` не ограничено.

## Запуск

```bash
cargo run --release
```

либо напрямую скомпилированный файл:

```bash
./target/release/tagvox
```

## Команды в Discord

Управляющие команды работают с указанным префиксом (по умолчанию `!`) и не
требуют упоминания бота:

| Команда                 | Действие                                              |
|--------------------------|--------------------------------------------------------|
| `!join`                  | подключиться к голосовому каналу автора сообщения      |
| `!leave`                 | отключиться от голосового канала                       |
| `!pause`                 | поставить текущий трек на паузу (позиция сохраняется)  |
| `!resume` / `!continue`  | продолжить с сохранённой позиции                       |
| `!stop`                  | остановить и сбросить позицию                          |
| `!ping`                  | показать текущую задержку соединения с Discord         |

Реакции на упоминание и голосовые команды задаются в `phrases.json` и
срабатывают, когда бот упомянут в сообщении (если в `config.json` не
отключено требование упоминания).

## Кнопки управления

Когда бот запускает или возобновляет трек, под сообщением появляются
кнопки «⏸ Пауза», «▶ Продолжить», «⏹ Стоп». Они действуют в том же
канале, где была вызвана команда; ответ на нажатие приходит эфемерным
сообщением, видимым только нажавшему.

## Дебаг и диагностика

- Каждые 30 секунд в консоль выводится задержка соединения (пинг) по
  каждому шарду.
- При каждой попытке подключения к голосовому каналу в консоль выводится
  результат — успех или причина отказа с расшифровкой типичных причин
  (отсутствие прав, блокировка UDP-трафика антивирусом/файрволом/VPN,
  Stage-канал и т.д.).
- Для более подробных логов используется переменная окружения `RUST_LOG`:

  ```powershell
  $env:RUST_LOG="debug"; cargo run --release
  ```

## Веб-панель (в разработке)

В проекте есть заготовка веб-панели управления: статический фронтенд
(`web/`) поверх REST API на Axum, встроенного в тот же процесс, что и бот
(запускается фоновой задачей — так панель видит реальный пинг, список
серверов и текущие треки без обмена данными между процессами).

**Текущий статус:** все четыре фазы веб-панели реализованы. Два режима
входа на выбор при первом запуске (вопрос в консоли) — через Discord OAuth
или по общему паролю (хранится только в виде хеша Argon2). Полный CRUD
настроек (`config.json`), реакций, голосовых команд, фразы-заглушки и
звуковых файлов (drag&drop загрузка, прослушивание, удаление) работает.
Изменения из панели применяются к работающему боту сразу (без перезапуска) и
одновременно сохраняются в `config.json`/`phrases.json`/`sounds/` на диске.
Полный контракт — [`web/API.md`](./web/API.md).

- `panel.toml` — настройки панели (порт, Discord OAuth credentials, секрет
  сессии), создаётся автоматически при первом запуске. Содержит секреты —
  исключён в `.gitignore`.
- `panel.db` — SQLite-база для настроек и сессий панели, тоже создаётся
  автоматически и исключена из git.
- Отключить панель полностью: `enabled = false` в `panel.toml`.

## Протокол DAVE (обязательное сквозное шифрование голоса)

С марта 2026 года Discord требует, чтобы клиент, подключающийся к обычному
(не Stage) голосовому каналу, поддерживал протокол DAVE (Audio & Video
End-to-End Encryption). При отсутствии поддержки соединение закрывается с
кодом `4017` и причиной `E2EE/DAVE protocol required`. Поддержка DAVE
появилась в `songbird` начиная с версии 0.6.0 — актуальная версия уже
указана в `Cargo.toml`.

## Структура проекта

```
tagvox/
├── Cargo.toml
├── build.rs              # копирует phrases.json, sounds/ и web/ в target/<profile> при сборке
├── phrases.json           # реакции, голосовые команды, фраза-заглушка
├── sounds/                 # локальные аудиофайлы для голосовых команд
├── web/                     # статический фронтенд веб-панели (см. web/API.md)
├── migrations/               # SQLx-миграции SQLite (встраиваются в бинарник при сборке)
├── LICENSE
├── .gitignore
└── src/
    ├── main.rs
    ├── config.rs           # загрузка/создание config.json
    ├── panel_config.rs     # загрузка/создание panel.toml
    ├── db.rs                # инициализация SQLite и миграций
    ├── token_store.rs      # запрос и хранение токена
    ├── phrases.rs          # загрузка phrases.json и нечёткое сравнение фраз
    ├── state.rs            # общее состояние приложения (для обработчика Discord)
    ├── web/mod.rs           # Axum-роутер и состояние веб-панели
    └── handler.rs          # обработка сообщений, кнопки, воспроизведение
```

Файлы `config.json`, `token.json`, `panel.toml` и `panel.db` в структуру не
включены — они создаются автоматически при первом запуске и не должны
попадать в репозиторий.

## Лицензия

Проект распространяется под лицензией **GNU Affero General Public License
v3.0** — полный текст в файле [`LICENSE`](./LICENSE).

Коротко: код можно свободно использовать, изучать, изменять и
распространять, в том числе в коммерческих целях. Главное условие AGPL —
если модифицированная версия используется как публично доступный сетевой
сервис (например, бот развёрнут и предоставляется другим как услуга), её
исходный код тоже должен быть открыт пользователям этого сервиса. Это не
юридическая консультация — при сомнениях стоит свериться с полным текстом
лицензии.